import{AVOMIEZ_LOCK,callM3,enforcePost,send}from'../lib/gmi.js';
import{normalizeVideoDuration}from'../lib/duration-policy.js';
const system=`You are the inspiration engine inside AvoMiez Director V2.8. ${AVOMIEZ_LOCK}\nGenerate highly distinct 5–30 second short-form video ideas that are visually clear, immediately understandable and realistic for modern AI video generation. Favor a strong opening hook, readable action progression, payoff and loop potential. For 16–30 second ideas, add meaningful progression rather than filler. Avoid copyrighted characters, politics, dangerous challenges and concepts that need long dialogue. Return JSON only.`;
function user({mode='random',count=5,duration=12,note=''}){const d=normalizeVideoDuration(duration);return`Generate exactly ${count} AvoMiez short-video ideas. Mode: ${mode}. Target duration: exactly ${d}s. Creator note: ${note||'none'}. Return exactly {"ideas":[{"title":"","category":"","idea":"complete editable idea in 1-4 concise sentences","hook":"opening 1.5 seconds","duration":${d},"difficulty":"easy|medium|hard","loopPotential":"low|medium|high"}]}. Every idea must be meaningfully different. For longer durations, use a clear beginning → escalation → payoff structure without padding.`}
export default async function handler(req,res){if(!enforcePost(req,res))return;try{const b=req.body||{},duration=normalizeVideoDuration(b.duration);const r=await callM3({system,user:user({...b,duration}),temperature:.95,maxTokens:1800});return send(res,200,{...r.data,_meta:{model:r.model,usage:r.usage,duration}})}catch(e){return send(res,500,{error:e.message||'Unexpected server error'})}}
