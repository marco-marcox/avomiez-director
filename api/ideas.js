import{AVOMIEZ_LOCK,callM3,enforcePost,send}from'../lib/gmi.js';
const system=`You are the inspiration engine inside AvoMiez Director. ${AVOMIEZ_LOCK}\nGenerate highly distinct 8–15 second short-form video ideas that are visually clear, immediately understandable and realistic for MiniMax H3. Favor a strong first-1.5-second hook, simple action choreography, payoff and loop potential. Avoid copyrighted characters, politics, dangerous challenges, dense crowds and concepts that need long dialogue. Return JSON only.`;
function user({mode='random',count=5,duration=12,note=''}){return`Generate exactly ${count} AvoMiez short-video ideas. Mode: ${mode}. Target duration: ${duration}s. Creator note: ${note||'none'}. Return exactly {"ideas":[{"title":"","category":"","idea":"complete editable idea in 1-3 sentences","hook":"first 1.5 seconds","duration":${duration},"difficulty":"easy|medium|hard","loopPotential":"low|medium|high"}]}. Every idea must be meaningfully different.`}
export default async function handler(req,res){if(!enforcePost(req,res))return;try{const r=await callM3({system,user:user(req.body||{}),temperature:.95,maxTokens:1800});return send(res,200,{...r.data,_meta:{model:r.model,usage:r.usage}})}catch(e){return send(res,500,{error:e.message||'Unexpected server error'})}}
