import{AVOMIEZ_LOCK,callM3,enforcePost,send}from'../lib/gmi.js';
import{VIDEO_PROMPT_MAX_CHARS,enforceVideoPromptLimit}from'../lib/prompt-limit.js';
import{toneOverlayText,normalizeToneOverlay}from'../lib/tone-overlay.js';
import{normalizeVideoDuration,durationPromptGuidance,softPromptTargetForDuration}from'../lib/duration-policy.js';
const system=`You are the refinement editor inside AvoMiez Director V2.8. ${AVOMIEZ_LOCK}\nYou receive an already production-ready reference-to-video prompt and improve exactly one requested quality while preserving the core story, AvoMiez identity, duration, 9:16 format, continuity, reference roles, active secondary tone overlay and practical model reliability. Support custom 5–30 second duration. Longer videos should use leaner prompt prose, not more text. The final revised h3Prompt must never exceed ${VIDEO_PROMPT_MAX_CHARS} characters. Return JSON only.`;
const modeRules={funnier:'Increase visual comedy through clearer deadpan feline reactions, contrast, timing and payoff. Do not turn AvoMiez cartoonish or add dialogue.',cinematic:'Increase cinematic impact through stronger composition, camera choreography, lighting, depth, pacing and visual payoff without bloating the prompt.',action:'Increase readable physical action and momentum while keeping choreography feasible and character-consistent.',loop:'Restructure the ending so it naturally reconnects to the opening when appropriate.',simplify:'Reduce generation risk. Remove redundant adjectives, excessive props, competing actions and unnecessary technical jargon. Preserve the core idea and strongest beats.'};
function user(b){const d=normalizeVideoDuration(b.duration),soft=softPromptTargetForDuration(d),rule=modeRules[b.mode]||modeRules.simplify;return`REFINEMENT MODE: ${b.mode}\nINSTRUCTION: ${rule}\n\n${toneOverlayText(b.toneOverlay)}\n\nCURRENT IDEA:\n${b.idea||''}\n\nCURRENT H3 PROMPT:\n${b.currentPrompt||''}\n\nTARGET: exactly ${d}s, vertical 9:16, style ${b.style||'cinematic'}, audio ${b.audio||'native synchronized sound'}, loop requested: ${b.loop?'yes':'optional'}.\n\n${durationPromptGuidance(d)}\nTONE RULE: preserve the selected overlay as an additive secondary flavor. Do not accidentally erase it while improving another quality.\nHARD LIMIT: revised h3Prompt must be <= ${VIDEO_PROMPT_MAX_CHARS} characters; for ${d}s prefer about ${soft} or less. Preserve reference IDs, character identity, active tone overlay, exact timing and core choreography before decorative wording.\n\nReturn exactly {"h3Prompt":"the complete revised production-ready prompt","changeSummary":"one concise sentence describing what changed"}. Keep @image1 as the strict AvoMiez master identity reference when mapped. Return a complete replacement prompt, not notes or a patch.`}
export default async function handler(req,res){if(!enforcePost(req,res))return;try{const b=req.body||{};if(!b.currentPrompt)return send(res,400,{error:'No H3 prompt available to improve.'});const duration=normalizeVideoDuration(b.duration),tone=normalizeToneOverlay(b.toneOverlay);const r=await callM3({system,user:user({...b,duration,toneOverlay:tone}),temperature:.45,maxTokens:2900});const data={...r.data};const fit=await enforceVideoPromptLimit(data.h3Prompt,{label:'refined video prompt',duration});data.h3Prompt=fit.prompt;return send(res,200,{...data,_meta:{model:r.model,usage:r.usage,duration,toneOverlay:tone,promptLimit:VIDEO_PROMPT_MAX_CHARS,promptSoftTarget:fit.softTarget,promptChars:fit.finalChars,promptOriginalChars:fit.originalChars,promptCompacted:fit.compacted,promptCompaction:fit.method}})}catch(e){return send(res,500,{error:e.message||'Unexpected server error'})}}
