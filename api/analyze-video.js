import{AVOMIEZ_LOCK,callM3,enforcePost,send}from'../lib/gmi.js';
const system=`You are the Source Video Analyst inside AvoMiez Director V2.3. You receive representative frames extracted from a user-uploaded video. Infer only what is visually supported by the frames and user note. Separate OBSERVATION from CREATIVE REIMAGINING. Identify the first-frame hook, setting, visible subjects, action progression, camera mechanics, visual mechanics, retention mechanics, payoff, ending and likely loop structure. Then create one NEW editable AvoMiez-compatible direction inspired by the mechanics without copying protected characters, logos or distinctive copyrighted expression. ${AVOMIEZ_LOCK}\nReturn JSON only.`;
function text({goal='mechanics',note='',duration=12}){return`Analyze the sampled frames in chronological order. GOAL: ${goal}. USER NOTE: ${note||'none'}. TARGET NEW VIDEO: ${duration}s vertical 9:16 for MiniMax H3 / PixVerse. If something is ambiguous, explicitly say it is ambiguous rather than inventing details.\n\nReturn exactly {\n"sourceSummary":"what objectively happens in the sampled video",\n"hook":"what makes the opening visually interesting",\n"setting":"",\n"visibleSubjects":[""],\n"keyActions":[""],\n"visualStyle":"",\n"visualMechanic":"the repeatable visual/content mechanism underneath the video",\n"cameraMechanic":"camera movement, framing progression and edit rhythm inferred from frames",\n"retentionMechanic":"why a viewer may keep watching, stated cautiously",\n"ending":"visible or inferred final beat; mark uncertain if necessary",\n"loopObservation":"whether the structure could loop and why",\n"newIdea":"editable new AvoMiez idea inspired by the mechanics",\n"continuationIdea":"editable directly connected next chapter",\n"h3Prompt":"baseline production-ready H3 prompt using @image1 as strict AvoMiez identity reference",\n"editNotes":[""]\n}. Do not claim guaranteed virality.`}
export default async function handler(req,res){if(!enforcePost(req,res))return;try{const b=req.body||{},frames=Array.isArray(b.frames)?b.frames.slice(0,6):[];if(!frames.length)return send(res,400,{error:'No video frames received.'});const content=[{type:'text',text:text(b)},...frames.map(url=>({type:'image_url',image_url:{url},detail:'low'}))];const r=await callM3({messages:[{role:'system',content:system},{role:'user',content}],temperature:.42,maxTokens:3000});return send(res,200,{...r.data,_meta:{model:r.model,usage:r.usage,framesAnalyzed:frames.length}})}catch(e){return send(res,500,{error:e.message||'Unexpected server error'})}}
